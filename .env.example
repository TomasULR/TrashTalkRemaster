# Zkopíruj na .env a vyplň před `docker compose up` v produkci.
# V dev necháváš výchozí hodnoty (compose má defaulty).

POSTGRES_PASSWORD=change-me-in-prod
MINIO_ROOT_USER=trashtalk
MINIO_ROOT_PASSWORD=change-me-in-prod-min-8-chars
